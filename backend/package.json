{
  "name": "marimar-saving-circles-backend",
  "version": "1.0.0",
  "description": "Backend API for Marimar Saving Circles",
  "main": "dist/server.js",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.production.json && node fix-imports.js",
    "start": "if [ ! -f dist/server.js ]; then echo 'Building TypeScript...'; bun run build; fi && node dist/server.js",
    "lint": "eslint src/**/*.ts",
    "format": "prettier --write .",
    "type-check": "tsc --noEmit",
    "test": "jest",
    "db:generate": "drizzle-kit generate",
    "db:push": "drizzle-kit push",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "db:seed": "tsx scripts/seed.ts",
    "db:simulate": "tsx scripts/simulate-selections.ts"
  },
  "dependencies": {
    "@hono/node-server": "^1.0.0",
    "@hono/zod-validator": "^0.3.0",
    "@vercel/node": "^3.0.0",
    "dotenv": "^16.3.1",
    "drizzle-orm": "^0.29.4",
    "hono": "^4.0.0",
    "paseto": "^3.1.4",
    "postgres": "^3.4.3",
    "sharp": "^0.34.5",
    "tsx": "^4.7.0",
    "typescript": "^5.0.0",
    "zod": "^3.22.4"
  },
  "keywords": [
    "saving-circles",
    "api",
    "hono",
    "typescript"
  ],
  "author": "Marimar Team",
  "license": "MIT",
  "devDependencies": {
    "tsc-alias": "^1.8.16"
  }
}
